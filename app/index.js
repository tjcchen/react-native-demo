import { View, Text } from 'react-native';
import { Link, Stack } from 'expo-router';

const Home = () => {
  return (
    <View>
      <Text>Home Let's write some code!</Text>
    </View>
  );
};

export default Home;